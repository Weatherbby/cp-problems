#include <bits/stdc++.h>
using namespace std;

int main(){
	int cases;
	cin >> cases;
	for(int i = 0; i < cases; i++){
		int n;
		cin >> n;
		std::vector<int> v(n);
		for(auto it = v.begin(); it != v.end(); ++it){
			
		}

	}	
}